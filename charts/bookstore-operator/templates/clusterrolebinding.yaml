apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "bookstore-operator.fullname" . }}-manager-rolebinding
  labels:
    {{- include "bookstore-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "bookstore-operator.fullname" . }}-manager-role
subjects:
- kind: ServiceAccount
  name: {{ include "bookstore-operator.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
