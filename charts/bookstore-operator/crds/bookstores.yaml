---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: bookstores.store.mylab.local
spec:
  group: store.mylab.local
  names:
    kind: BookStore
    listKind: BookStoreList
    plural: bookstores
    singular: bookstore
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BookStore is the Schema for the bookstores API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of BookStore
            properties:
              image:
                default: nginx:1.25
                description: Container image
                type: string
              name:
                description: Name of the bookstore
                minLength: 1
                type: string
              port:
                default: 80
                description: Application port
                format: int32
                maximum: 65535
                minimum: 1
                type: integer
              replicas:
                default: 1
                description: Number of application replicas
                format: int32
                minimum: 1
                type: integer
            required:
            - name
            type: object
          status:
            description: status defines the observed state of BookStore
            properties:
              phase:
                type: string
              readyReplicas:
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
